import { UploadImageProcessor } from '../../common/processors/upload-image/upload-image.processor';
import { UploadVideoProcessor } from '../../common/processors/upload-video/upload-video.processor';

export const Processors = [UploadVideoProcessor, UploadImageProcessor];
