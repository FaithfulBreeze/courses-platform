import { AppendCookieInterceptor } from './append-cookie.interceptor';

describe('AppendCookieInterceptor', () => {
  it('should be defined', () => {
    expect(new AppendCookieInterceptor()).toBeDefined();
  });
});
